using System;

namespace CsharpExercicios;
class Program
{
    static void Main(string[] args)
    {
        ApresentarPrograma();
        Calculo();
    }

    public static void ApresentarPrograma()
    {
        Console.WriteLine("Programa do desconto em porcentagem");
    }

    public static void Calculo()
    {
        double valorCompra = 1000;
        double porcentagem = 1.5;

        double desconto = valorCompra * (porcentagem / 100);
        double total = valorCompra - desconto;

        Console.WriteLine($"Compra finalizada! O total é de R$ {total}");
    }
}
